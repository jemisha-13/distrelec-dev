# ImpEx for Importing Products

$productCatalog=distrelecProductCatalog
$productCV=catalogVersion(catalog(id[default=$productCatalog]),version[default='Online'])


INSERT_UPDATE DistManufacturer;code[unique=true];name;primaryImage(qualifier,$productCV);promotionText[lang=de];
;manufacturer_sample;Manufacturer Sample;placeholdersMediaContainer;Hier mein Promo Text;
