import { DistIcon } from '@model/icon.model';

export const bullet: DistIcon = {
  name: 'bullet',
  viewbox: '0 0 8 8',
  dimensions: [8, 8],
  colour: 'none',
  elements: [{ type: 'element', tagName: 'circle', properties: { cx: 4, cy: 4, r: 4, fill: '#2F6FED' }, children: [] }],
};
