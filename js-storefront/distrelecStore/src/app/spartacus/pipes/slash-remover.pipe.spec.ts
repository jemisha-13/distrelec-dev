import { SlashRemoverPipe } from './slash-remover.pipe';

describe('SlashRemoverPipe', () => {
  it('create an instance', () => {
    const pipe = new SlashRemoverPipe();
    expect(pipe).toBeTruthy();
  });
});
