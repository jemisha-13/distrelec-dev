import { NgModule } from '@angular/core';
import { RemoveHostDirective } from '@features/shared-modules/directives/remove-host.directive';

@NgModule({
  imports: [],
  declarations: [RemoveHostDirective],
  exports: [RemoveHostDirective],
})
export class RemoveHostDirectiveModule {}
