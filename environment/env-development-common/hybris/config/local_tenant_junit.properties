jms.broker.url=tcp://localhost:61617?persistent=true

spring.profiles.active=adm,test,tenant_junit

# disable global audit
auditing.enabled=false

# use j table prefix instead of default junit_
db.tableprefix=j
